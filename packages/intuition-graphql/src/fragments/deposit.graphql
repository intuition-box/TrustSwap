fragment DepositEventFragment on events {
  deposit {
    term_id
    curve_id
    sender_assets_after_total_fees
    shares_for_receiver
    receiver {
      id
    }
    sender {
      id
    }
  }
}
