{
  "name": "intuition-uniswapv2-fork-workspace",
  "private": true,
  "workspaces": [
    "contracts",
    "frontend"
  ],
  "scripts": {
    "build": "pnpm -C contracts build && pnpm -C frontend build",
    "dev:fe": "pnpm -C frontend dev",
    "dev:hh": "pnpm -C contracts hardhat",
    "test": "pnpm -C contracts test",
    "clean": "rimraf node_modules contracts/node_modules frontend/node_modules && rimraf contracts/cache contracts/artifacts"
  },
  "devDependencies": {
    "rimraf": "^6.0.0"
  }
}
